# 巡检目标与指标

## 2.1 核心目标

- 提前发现集群软件异常（如服务崩溃、配置错误）、性能瓶颈（如 CPU、内存、磁盘使用率过高）及安全隐患；
- 确保集群各项运行指标符合预设标准，服务可用性、响应速度满足业务需求；
- 建立巡检档案，形成故障排查、性能优化的闭环管理，持续提升集群运维质量；
- 降低人工巡检成本，提升运维效率，实现集群运维的规范化、智能化。

## 三、巡检内容与标准

### 3.1 基础通用巡检项（集群通用）

#### 3.1.1 巡检标准

| 检查项 | 检查方法 | 合格标准 |
| --- | --- | --- |
| ssh 连通 | ping / ssh 远程执行命令等 | 正常 ping 通或命令可执行 |
| 运行时长/天 | `uptime` 命令等 | / |
| 时间同步状态 | 管理节点和巡检节点分别获取时间 | 相差时间 < 30s |
| 防火墙状态 | `firewalld.service` 状态 | / |
| SELinux 状态 | `getenforce` | / |
| 节点根目录占比 | `df -h` 等 | / |

#### 3.1.2 巡检内容展示示例

暂时无法在飞书文档外展示此内容。

> 注意事项：
>
> 1. ssh 连通异常，无需重复执行剩余检查项，标记 `\`；
> 2. 运行时长不足一天按一天计算。

### 3.2 HPC 巡检项

#### 3.2.1 slurm + gpfs 集群

##### 3.2.1.1 巡检标准

| 检查项 | 检查方法 | 合格标准 |
| --- | --- | --- |
| 节点调度状态 | `scontrol show node <节点名>` | / |
| 用户同步状态 | `id <用户名>` | 存在/不存在 |
| IB 网络状态 | `ibstat`（特定端口状态） | Active / Down |
| 文件系统挂载状态 | `df -h` 等 | / |
| RDMA | `mmfsadm test verbs status` / `mmfsadm test verbs conns` | / |

##### 3.2.1.2 巡检内容展示示例

暂时无法在飞书文档外展示此内容。
